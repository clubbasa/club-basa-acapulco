import {NextResponse} from 'next/server';
export async function POST(req:Request){const body=await req.json().catch(()=>null);if(!body?.name||!body?.contact)return NextResponse.json({ok:false,error:'Datos incompletos'},{status:400});return NextResponse.json({ok:true,message:'Contacto recibido'});}