'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {onAuthStateChanged} from 'firebase/auth';import {doc,getDoc} from 'firebase/firestore';import {auth,db} from '@/lib/firebase';
export default function Admin(){const[ok,setOk]=useState(false);const[loading,setLoading]=useState(true);useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){setLoading(false);return}const s=await getDoc(doc(db,'users',u.uid));setOk(s.data()?.role==='admin'&&s.data()?.status==='active');setLoading(false)}),[]);if(loading)return <main className="container py-16">Verificando acceso…</main>;if(!ok)return <main className="container py-16"><h1 className="display text-4xl font-bold">Acceso restringido</h1><p className="mt-3">Necesitas una cuenta de administrador activa.</p></main>;return <main className="container py-16"><h1 className="display text-5xl font-bold">Administración</h1><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{[['/admin/usuarios','Usuarios'],['/admin/productos','Productos'],['/admin/pendientes','Pendientes'],['/admin/administracion','Configuración'],['/admin/mi-cuenta','Mi cuenta']].map(([href,label])=><Link key={href} href={href} className="rounded-3xl bg-white p-7 text-xl font-bold shadow-soft">{label} →</Link>)}</div></main>}