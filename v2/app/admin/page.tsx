'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {onAuthStateChanged} from 'firebase/auth';import {doc,getDoc} from 'firebase/firestore';import {auth,db} from '@/lib/firebase';
export default function Admin(){const[ok,setOk]=useState(false);const[loading,setLoading]=useState(true);const[force,setForce]=useState(false);useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){setLoading(false);return}const d=(await getDoc(doc(db,'users',u.uid))).data();setOk(d?.role==='admin'&&d?.status==='active');setForce(d?.forcePasswordChange===true);setLoading(false)}),[]);if(loading)return <main className="container py-16">Verificando acceso…</main>;if(!ok)return <main className="container py-16"><h1 className="display text-4xl font-bold">Acceso restringido</h1><p className="mt-3">Necesitas una cuenta de administrador activa.</p></main>;if(force)return <main className="container py-16"><div className="mx-auto max-w-xl rounded-3xl bg-oat p-8"><h1 className="display text-4xl font-bold">Cambia tu contraseña</h1><p className="mt-3">Por seguridad, el primer acceso debe terminar con un cambio de contraseña.</p><Link href="/admin/mi-cuenta" className="mt-6 inline-block rounded-full bg-toast px-5 py-3 font-bold text-white">Cambiar contraseña</Link></div></main>;return <main className="container py-16"><h1 className="display text-5xl font-bold">Administración</h1><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{[['/admin/usuarios','Usuarios'],['/admin/productos','Productos'],['/admin/pendientes','Pendientes'],['/admin/administracion','Configuración'],['/admin/mi-cuenta','Mi cuenta']].map(([href,label])=><Link key={href} href={href} className="rounded-3xl bg-white p-7 text-xl font-bold shadow-soft">{label} →</Link>)}</div></main>}