'use client';
import {useCart} from './cart-provider';
import type {Product} from '@/lib/demo-data';
export function ProductCard({p}:{p:Product}){const{add}=useCart();return <article className="overflow-hidden rounded-3xl bg-white shadow-soft"><div className="aspect-[4/3] bg-oat flex items-center justify-center overflow-hidden">{p.image?<img src={p.image} alt={p.name} className="h-full w-full object-cover"/>:<span className="display text-5xl text-toast/30">BASA</span>}</div><div className="p-5"><div className="text-xs font-bold uppercase tracking-widest text-mint">{p.category}</div><h3 className="display mt-1 text-2xl font-bold">{p.name}</h3><p className="mt-2 text-sm leading-6 text-ink/65">{p.description}</p><div className="mt-5 flex items-center justify-between"><strong className="text-xl">${p.price.toFixed(2)} MXN</strong><button onClick={()=>add({...p,qty:1})} className="rounded-full bg-mint px-4 py-2 font-bold text-white">Agregar</button></div></div></article>}